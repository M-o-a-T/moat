#!/usr/bin/make -f

export PYBUILD_NAME=moat-link-metrics
%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_install:
	dh_auto_install
	install -D -m 644 systemd/moat-link-metrics@.service \
		debian/moat-link-metrics/usr/lib/systemd/system/moat-link-metrics@.service
